/**
 * Authentication utilities compatible with Satchel Auth^2
 * Supports magic link and passkey authentication only
 * NO password-based authentication
 */

// Export auth utilities as they are created
// Example:
// export { createMagicLink, verifyMagicLink } from './magicLink';
// export { createPasskey, verifyPasskey } from './passkey';
// export { AuthProvider, useAuth } from './AuthContext';

export {};